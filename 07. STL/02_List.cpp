/*“So, be patient. Surely Allah’s promise is true, and let not the disbelievers shake your firmness” (Quran, 30:60)*/
#include <bits/stdc++.h>
#define ll long long
using namespace std;
int main()
{
    ios_base ::sync_with_stdio(0);
    cin.tie(0);

    list<int> l;
    l.push_front(1);
    l.push_back(2);
    l.push_back(3);

    // for (auto x : l)
    // {
    //     cout << x << " ";
    // }
    // cout << endl;

    // cout << l.size() << endl;

    // list<int> l2 = l;
    // for (auto x : l2)
    // {
    //     cout << x << " ";
    // }
    // cout << endl;

    return 0;
}

// Time complexity of operations : O(n);