/*“So, be patient. Surely Allah’s promise is true, and let not the disbelievers shake your firmness” (Quran, 30:60)*/
#include <bits/stdc++.h>
#define ll long long
using namespace std;
int main()
{
    ios_base ::sync_with_stdio(0);
    cin.tie(0);

    deque<int> deq;
    deq.push_back(2);
    deq.push_front(1);

    // for (auto x : deq)
    // {
    //     cout << x << " ";
    // }
    // cout << endl;

    // deq.pop_back();
    // for (auto x : deq)
    // {
    //     cout << x << " ";
    // }
    // cout << endl;

    // cout << deq.at(0) << endl;

    // cout << "Empty: " << deq.empty() << endl;

    // deq.erase(deq.begin(), deq.begin() + 1);   // erases begin to first element
    // for (auto x : deq)
    // {
    //     cout << x << " ";
    // }
    // cout << endl;

    // cout << deq.size() << endl;

    return 0;
}

// All operation complexity : O(1);
